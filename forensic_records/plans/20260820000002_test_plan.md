# Test Plan: 20260820000002_fix_location_label_rpc.sql

## Goal
Verify that the `read_infraction_context` RPC prioritizes textual location labels (e.g. `geofence_name`, `address`) over numerical coordinates.

## Scope
- Update `read_infraction_context` RPC.

## Checks
1. [x] Prioritizes `geofence_name` if present.
2. [x] Falls back to `address` or `location_label`.
3. [x] Falls back to `lat,lng` string only when no textual labels exist.
