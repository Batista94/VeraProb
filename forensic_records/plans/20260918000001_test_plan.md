# Forensic Test Plan: Harden Default Tenant Role Permissions (20260918000001)

## Context
Refactoring of the `_seed_default_tenant_roles` function to enforce Segregation of Duties (SoD) by removing `users:manage` from the "Validador" profile, and Principle of Least Privilege (PoLP) by removing `contracts:write` from the "Operador" profile.

## Scope
- `supabase/migrations/20260918000001_harden_default_tenant_role_permissions.sql`
- `supabase/tests/20260918000001_harden_default_tenant_role_permissions_test.sql`

## Invariants
- **INV-DB**: Append-only migration. `CREATE OR REPLACE FUNCTION` is used.
- **INV-1/INV-22**: Organization ID continues to be filtered, maintaining tenant isolation.

## Assertions
1. Validador NO LONGER has `users:manage` permission.
2. Operador NO LONGER has `contracts:write` permission.
3. Validador has exactly 10 permissions.
4. Operador has exactly 5 permissions.
5. Auditor remains unchanged with exactly 6 permissions.
6. Administrador remains unchanged with ALL permissions.
7. Retroactive helper correctly applies the new matrices.
