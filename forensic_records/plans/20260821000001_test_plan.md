# Test Plan: 20260821000001_enhance_infraction_context_rpc.sql

**Governing Migration:** `20260821000001_enhance_infraction_context_rpc.sql`
**Invariants Checked:** INV-1, INV-26, INV-22

## Purpose
Ensures that `read_infraction_context` properly exposes the new `primary_evidence_lat`, `primary_evidence_lng`, and `clause_ref` properties for the Dispute Portal.

## Scenarios
1. **Valid Token Context**
   - Condition: A valid read or submit token is used.
   - Expected Output: The JSONB payload must contain `primary_evidence_lat`, `primary_evidence_lng` (as numeric) and `clause_ref` string matching the inner evidence snapshot.

2. **Error Parity (INV-26)**
   - Condition: Token is invalid, revoked, or max access count reached.
   - Expected Output: Must raise `insufficient_privilege` (42501).

3. **Tenant Isolation (INV-22)**
   - Condition: The token belongs to an org, but the underlying queue entry was mutated to another org (impossible under normal use but tested).
   - Expected Output: Must raise `insufficient_privilege` (42501).
