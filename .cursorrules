# VeraProb — Cursor Rules

> **ATTENTION TO CURSOR AGENT (COMPOSER 2.5):**
> This repository has migrated to **modular rules (.mdc)** in the `.cursor/rules/` directory.
> 
> You do not need to read all manuals in the chat, and you **should not try to run strict TDD flows or write "Step 0" blocks manually**. Simply trust your internal agent loop. Specific technology and business rules will be automatically injected into your context depending on the files you are editing.

**For absolute references, SSOTs, and universal rules not covered by `.mdc` files, consult the official repository index in `AGENTS.md`.**
