name: VeraProb Forensic Gatekeeper

on:
  pull_request:
    branches: [ main, develop ]

jobs:
  forensic-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install Dependencies
        run: flutter pub get

      - name: Run Forensic Scanner
        run: |
          chmod +x scripts/*.sh
          BASE_BRANCH=${{ github.base_ref }} bash scripts/pr_full_scanner.sh
